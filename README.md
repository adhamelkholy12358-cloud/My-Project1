# My-Project1

This project demonstrates a simple ETL pipeline that extracts data from CSV and JSON files, transforms and cleans the data using Pandas, and loads the cleaned data into a CSV file.

Technologies
Python
Pandas
Logging

ETL Process

Extract
Read CSV and JSON files

Transform
Clean missing values
Fix product names
Calculate total value

Load
Save cleaned data into CSV
